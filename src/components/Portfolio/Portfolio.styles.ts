import styled from 'styled-components'

const PortfolioSection = styled.section`
  margin-bottom: 40px;
`

const AssetAllocationChart = styled.div`
  background-color: #F1EFEF;
  padding: 25px;
  border-radius: 8px;
  margin-bottom: 25px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
`

const HoldingsTable = styled.div`
  background-color: #F1EFEF;
  padding: 25px;
  border-radius: 8px;
  margin-bottom: 25px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
`

const PerformanceChart = styled.div`
  background-color: #F1EFEF;
  padding: 25px;
  border-radius: 8px;
  margin-bottom: 25px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
`

export default { PortfolioSection, AssetAllocationChart, HoldingsTable, PerformanceChart }