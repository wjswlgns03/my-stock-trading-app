import styled from 'styled-components'

const StrategySection = styled.section`
  margin-bottom: 40px;
`

const StrategyWizard = styled.div`
  background-color: #F1EFEF;
  padding: 25px;
  border-radius: 8px;
  margin-bottom: 25px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
`

const VisualEditor = styled.div`
  // 비주얼 에디터 스타일
`

const BacktestResults = styled.div`
  // 백테스트 결과 스타일
`

export default { StrategySection, StrategyWizard, VisualEditor, BacktestResults }